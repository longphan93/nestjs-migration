export enum ColumnTypeEnum {
  UUID = 'uuid',
  STRING = 'string',
  TEXT = 'text',
  INTEGER = 'integer',
  BOOLEAN = 'boolean',
  TIMESTAMP = 'timestamp',
  FLOAT = 'float',
  DOUBLE = 'double',
  DECIMAL = 'decimal',
}
